# Python-Dependencies fuer System A und System B
# Installation: pip install -r requirements.txt
requests>=2.31.0       # Ollama- und Fuseki-HTTP-Aufrufe (System A + B)
PyYAML>=6.0            # Benchmark-Dateien und results_summary
rdflib>=7.0            # Snapshot-Validierung (Tripelzahl, snapshot.ttl)
